version: "3.8"

services:
  node:
    build:
      context: .
      dockerfile: dockerfile
    depends_on:
      - redis

    networks:
      - redisnet  

  redis:
    image: redis:latest
    networks:
      - redisnet

    ports:
      - "6379:6379"

networks:
  redisnet: